export enum ButtonVariants {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  MODAL = 'modal'
}

export enum InputVariants {
  PRIMARY = 'primary'
}

export enum CollectionViewCellVariants {
  USERS = 'users'
}
